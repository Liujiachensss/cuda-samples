message(STATUS ${CUDA_LIBRARIES})
enable_language(CUDA)
add_executable(deviceQ deviceQuery/deviceQuery.cpp)
target_link_libraries(deviceQ PUBLIC CUDA::cudart)
# target_compile_options(deviceQ PRIVATE --ccbin)
